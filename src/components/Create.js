import { View, StyleSheet, Text } from 'react-native';

export const Create = ({ post, onOpen }) => {
    return (
        <View>
            <Text>Create Page</Text>
        </View>
    );
};

const styles = StyleSheet.create({});
