import { View, StyleSheet, Text } from 'react-native';

export const About = () => {
    return (
        <View>
            <Text>About Page</Text>
        </View>
    );
};

const styles = StyleSheet.create({});
